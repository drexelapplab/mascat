import os

class linkedlist:
	head = None

	def __init__(self,head=None):
		self.head = head

	def out():
		return 

	def next(self):
		if self.head.getNextNode() != None:
			self.head = self.head.getNextNode()
		else:
			return 0